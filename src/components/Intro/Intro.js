import React, { Component } from 'react';
import './Intro.scss';

class Footer extends Component {
  render() {
    return (
      <div>
        Test
      </div>
    );
  }
}

export default (Footer);
